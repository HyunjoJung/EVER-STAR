import React from 'react';
import { Login as LoginTemplate } from 'components/templates/Login';

/**
 * 로그인 페이지
 * AuthLayout에서 배경 이미지와 중앙 정렬 처리
 */
export const LoginPage: React.FC = () => {
  return <LoginTemplate />;
};
